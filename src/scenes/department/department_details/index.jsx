import { useTheme } from "@mui/material"



const DepartmentDetails = () => {
    const theme = useTheme();
    const colors = tokens(theme.palette.mode);

    return (
        <Box>
            {/* <Header title={""} subtitle={"Add new team Member"} /> */}
        </Box>
    )
}

export default DepartmentDetails;